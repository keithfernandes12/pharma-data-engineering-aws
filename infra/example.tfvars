# Template for Terraform variables. Copy to terraform.tfvars and fill in.
#   cp example.tfvars terraform.tfvars
# terraform.tfvars is gitignored so your real email is never committed.

alert_email        = "you@example.com"
aws_region         = "us-east-1"
monthly_budget_usd = 5
